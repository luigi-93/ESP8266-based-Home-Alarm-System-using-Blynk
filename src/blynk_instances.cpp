#include "blynk_handlers.h"
#include <BlynkSimpleEsp8266.h>

// This translation unit allows the Blynk library to instantiate the
// single global `Blynk` object. Other translation units include
// `include/blynk_extern.h` which defines `NO_GLOBAL_BLYNK` and
// declares `extern BlynkWifi Blynk;` so they reference this instance.

